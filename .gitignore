node_modules/
.cache/*
decoded-corpus/
*.bak
*.tmp
__pycache__/
.env
*.json
!package.json
!tsconfig.json
!netlify.toml
!web/assets/data/
!web/assets/data/search-corpus.json
# Specifically exclude service account keys
gcs-key.json
ops/gcs-key.json
# metadata is now in corpus/ and tracked
#!.cache/ijhs.tsv
#!.cache/ijhs-classified.tsv
#!.cache/ijhs-classified.md
*~*
_agent/
patra-darpan-spasta-corpus.code-workspace

# Local Netlify folder
.netlify
web/.netlify/

# Netlify CLI may create this when running from web/ with only ".netlify".
web/.gitignore

# PDF corpus subfolders are local mirrors of GCS; never commit
corpus/ijhs/
corpus/other/
web/assets/pdfs
# Specifically track the metadata TSVs
!corpus/*.tsv
corpus/*~*
# Intake staging — processed files and PDFs are transient
intake/pdfs/
intake/processed/

# Local audit-decoder review state
tools/audit-decoder/.state/
